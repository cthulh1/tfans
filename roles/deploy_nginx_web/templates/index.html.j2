<!DOCTYPE html>
<html>
<head>
<title>Welcome to index.html!</title>
<style>
    body {
        width: 35em;
        margin: 0 auto;
        font-family: Tahoma, Verdana, Arial, sans-serif;
    }
</style>
</head>
<body>
<h1>Test index.html generated by Ansible on {% if ansible_distribution == 'Ubuntu' %}Ubuntu{% elif ansible_distribution == 'CentOS' %}CentOS{% endif %}</h1>

Web users list:
{% if ansible_distribution == 'Ubuntu' %}
{% for data in web1_user_names %}
{{ data }}{{ ", " if not loop.last else "." }}
{% endfor %}
{% elif ansible_distribution == 'CentOS' %}
{% for data in web2_user_names %}
{{ data }}{{ ", " if not loop.last else "." }}
{% endfor %}
{% endif %}
</body>
</html>
